<li>
  <%= link_to property.title, property_path(property) %> | <%= property.bedrooms %> bedrooms for <%= number_to_currency(property.price) %> / night
  <div class="rent_options">
    <% if property.rented %>
      <p>Property has been rented.</p>
      <% if current_user %>
        <% if current_user.id == property.owner.id %>
          <%= link_to user_property_path(user_id: property.owner.id, property: {:rented => false}), method: "patch" do %>
            Make Available
          <% end %>
        <% end %>
      <% end %>
    <% else %>
      <p><i>Property available.</i></p>
      <% if current_user %>
       <span class="edit_links"><% if current_user.id == property.owner.id %>
          <%= link_to "Edit", edit_user_property_path(current_user, property), remote: true %> |
          <%= link_to "Remove", user_property_path(current_user, property), :data => {:confirm => "Are you certain?", :method => "delete"} %></span>
        <% end %>
      <% end %>
    <% end %>
  </div>
</li>
